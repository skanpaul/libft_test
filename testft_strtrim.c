#include "libft.h"
#include "libft_test.h"

/* ************************************************************************** */
void testft_strtrim(void)
{
	// int i;
	// char *set = "xy9";
	// char *text = "Ily fait beaux le matin9";

	// i = 0;
	// while (text[i] != '\0')
	// {
	// 	if (sk_isset(text[i], set))
	// 	{
	// 		printf("index: %d | text[i]: %c | OUI\n", i, text[i]);

	// 	}
	// 	else
	// 		printf("index: %d | text[i]: %c\n", i, text[i]);
	// 	i++;
	// }
}
